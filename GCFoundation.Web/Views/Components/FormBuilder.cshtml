@using GCFoundation.Web.Resources
@model GCFoundation.Web.Models.FormBuilderTestViewModel

<gcds-grid columns="minmax(0, 1fr)" columns-desktop="320px minmax(0, 1fr)" alignment="center" tag="div" container="GCFoundation.Components.Enums.SizeTypeEmum.xl" class="xl:px-0 sm:px-600 px-225">
    @* Side navigation *@
    @if (Model.ShowSideNavigation)
    {
        <partial name="_SideNavigation" model="Model.SideNavigation" />
    }
    <div class="documentation-content">
        <gcds-heading tag="h1">@Model.Name<br /><code>@Html.Raw(Html.Encode(Model.Tag))</code></gcds-heading>

        @* Overview *@
        <partial name="_Overview" model="Model" />

        @* Sample code section *@
        <partial name="FormBuilder/_SampleForm" model="Model" />

        @* Properties *@
        <partial name="_Properties" model="Model" />

        @* Notes *@
        <partial name="_Notes" model="Model" />
    </div>
</gcds-grid>